Two items queued in TriAPI's CARRYOVER.md for this repo, both final decisions, do not ask clarifying questions. Do NOT attempt to split any file into a package (a prior session tonight attempted splitting ohmyllama/state.py and 4 of 6 generated files turned out to be fabricated rewrites rather than faithful extractions -- that was reverted and is explicitly out of scope here).

1. tests/test_dep_triage_seam.py asserts fastapi is imported directly by webui.py ("fastapi IS imported directly (webui.py) -- survives litellm's removal" and "fastapi used_in includes webui.py specifically"), but webui.py is missing from the working tree right now. IMPORTANT, investigate this first before touching the test: `git status` shows `ohmyllama/webui.py` as `D` (deleted) -- the file IS still tracked in git (last touched by commit 2a1e974, 2026-08-16 21:17), so this is an UNCOMMITTED local deletion, not a file that was ever actually removed via a commit (`git log --diff-filter=D -- ohmyllama/webui.py` returns nothing). It is unknown whether this deletion was intentional (matches the general "openclaw/consolidation" pivot narrative -- plausible webui.py was meant to go) or accidental/stray from some earlier session. Do NOT assume either way -- investigate: check `git show 2a1e974:ohmyllama/webui.py` to see the file's last committed content and judge whether it looks like dead/superseded code (e.g. does it duplicate something that now lives elsewhere, like src/semai/), and check whether anything currently imports `ohmyllama.webui` (grep for it) -- if something does, the deletion is likely accidental and the file should probably be restored (`git checkout -- ohmyllama/webui.py`) rather than the test changed. If nothing imports it and it does look superseded/dead, finalize the deletion properly with `git rm ohmyllama/webui.py` (turning the stray working-tree deletion into a real, committed one) rather than leaving it as an ambiguous uncommitted state, THEN fix the test. Either way, once resolved: investigate where fastapi is ACTUALLY imported/used in the current codebase (grep for "import fastapi" / "from fastapi"), update the stale assertion(s) in tests/test_dep_triage_seam.py to check the real current file (or reflect fastapi's real current classification, informed by scripts/dep_triage.py's own logic, if it turns out to have zero real imports anywhere). Then remove the *test_dep_triage_seam* exclusion from run_tests.sh's case statement (restoring this file to the normal script-suite run) once it passes for real.

2. AGENTS.md (repo root) is oversized -- 214,797 chars, over run_tests.sh/TriAPI's own 73,728-char Tier-4 context ceiling -- from many appended `<!-- triapi:plan run_id=... start/end -->` history blocks accumulated across a long consolidation project, several of them fully superseded/never-dispatched runs from earlier tonight (run IDs 20260819-104204-8e48b5 and 20260819-123531-1665d3 specifically -- both explicitly superseded by later corrected runs and should never have been left in the file). A THIRD block, run_id=20260819-132222-9de752 (lines ~1337-1410), is also now dead and must be retired too: its sections 1-5 (secrets role/tier pins, the OMLL_MODEL_HEAVY ordered-fallback code change, chat-kind search grounding, openclaw 401 investigation) genuinely completed and are already checked off `[x]` -- leave those as historical record. But its section 6 ("Split ohmyllama/state.py into ohmyllama/state/ package", 9 unchecked items) was attempted, found to have fabricated 4-of-6 mixin files (not faithful extractions), and was reverted by hand back to the flat `ohmyllama/state.py` -- this section must never be dispatched as-is; a correct re-split is intentionally out of scope for THIS plan (per this prompt's opening line) and is tracked separately. Its section 7 ("Fix dead Fimbulvetr entry", 3 unchecked items) was also already applied by hand in a later session (both `DEFAULT_LITERATURE_MODELS` copies already fixed -- verify this is still true by grep for `BookWormXtreme` in `ohmyllama/config.py` and `src/semai/workers/ghostwriter.py`; if genuinely still stale, this plan should actually apply the fix, but expect it to already be done). Its section 8 (documentation) was never done because the plan stalled on section 6. Resolution: check off section 1-5's boxes if not already, delete sections 6/7/8 (and their now-inapplicable content) from the block or mark them explicitly superseded/abandoned inline, and fold anything from section 8 that's still relevant (e.g. the "no source file may reach/exceed 73728 chars" standing rule, if not already present elsewhere in AGENTS.md) into this plan's own item 2b below instead of leaving it as a dangling unchecked box. The goal is for `triapi status 20260819-132222-9de752`'s incomplete-plan gate to no longer block future `triapi plan` runs against this repo once this plan's own docs item is done -- do not just close the HTML comment early without addressing the unchecked boxes inside it, since the checkbox-count integrity guard (`agents_md_gate.mark_plan_complete`) refuses to mark a block done if checked boxes don't match dispatched items.
   a. Prune AGENTS.md: remove the fully-superseded/never-dispatched `<!-- triapi:plan -->` blocks for run IDs 20260819-104204-8e48b5 and 20260819-123531-1665d3 (locate their start/end HTML comment markers and delete everything between and including them), and resolve run_id=20260819-132222-9de752 per the paragraph above. For any OTHER appended plan block whose every checklist item is already checked off `[x]` (fully completed, historical), it is safe to remove those too, following this repo's own established convention (see AGENTS.md's own file/git history for prior instances of this exact kind of pruning). Do NOT remove or alter the "Living file/dir index", "Standing rule for anyone (human or agent) working in this repo", "Where to look", or "Consolidation Phase 5" sections -- those are current, load-bearing documentation, not history.
   b. Append a NEW, concise entry to AGENTS.md's "Living file/dir index" section (following its existing one-line-per-entry format) documenting tonight's changes: the 7 secrets.json role pins (OMLL_MODEL_ROLE_ROUTER/AGENT/DATES/SUMMARISE/CODE/TRIAGE/CRITIC); OMLL_MODEL_HEAVY's new ordered-fallback-list design via the new Config.heavy_fallbacks() method (distinct from models_for(), which resolves roles not tiers -- note this distinction, it caused a real bug); the new chat-kind web-search-grounding branch in ohmyllama/orchestrator.py's _answer() (via ohmyllama/capabilities/search_router.py's SearchRouter); the corrected DEFAULT_LITERATURE_MODELS third entry (now hf.co/Sao10K/Fimbulvetr-11B-v2-GGUF:Q4_K_M) in both ohmyllama/config.py and src/semai/workers/ghostwriter.py; and the read-time quarantine-expiry pruning in ohmyllama/state.py's model_health(). Keep this concise -- one or two lines per item, no narrative, matching this file's own doc-hygiene convention (see its "Standing rule" section).

Also, informational only, no code change needed for this: after pruning AGENTS.md, run `wc -c ohmyllama/orchestrator.py ohmyllama/cli.py` and report both sizes in the plan's own verification output -- confirming whether either has crossed the 73,728-char Tier 4 ceiling now that this session added the search-grounding branch to orchestrator.py (both were ~52,000 chars before tonight's changes). If either has crossed it, do NOT attempt to split it -- just report the finding, matching the same "don't retry the failed split approach" caveat as item 1's file above.

Test command: `bash run_tests.sh` (must be run from the oh-my-llama repo root; this repo, unlike TriAPI, uses this script, not `python3 -m unittest`). Every secrets.json-adjacent claim in this plan (the role pin names, the OMLL_MODEL_HEAVY design) is informational context from a prior session, not something this plan touches -- this plan does not edit .secret/secrets.json at all.
