# Queued plan — TriAPI CARRYOVER items #1–#3

**Status: generated and reviewed (sound — correct line refs, correct
design), never approved/dispatched.** Run `20260819-204244-ac6c06`
(`logs/runs/20260819-204244-ac6c06.json`, `status: failed`, `plan_text:
null` — the plan text below only exists here, the run's own JSON never
got to record it since approval never happened).

## To resume

Run this from the TriAPI repo root, review the plan it reprints (should
match the text below — Tier 1 planning is not perfectly deterministic, so
diff mentally before approving), type `approve`, then `triapi dispatch
<new-run-id>`:

```
cd ~/Documents/Coding/TriAPI
PROMPT="$(cat queued_plans/triapi_items_1-3_prompt.txt)"
python3 scripts/triapi.py plan --project-dir /home/dyne/Documents/Coding/TriAPI "$PROMPT"
```

The exact prompt used is saved verbatim in `triapi_items_1-3_prompt.txt`
next to this file.

## The reviewed plan text (run 20260819-204244-ac6c06)

1. Phase 1: Fix `breakdown_plan()` re-running post-breakdown guards on resume
   - [ ] In `scripts/dispatcher.py`, modify `breakdown_plan()` (currently `def breakdown_plan(state: dict) -> dict:` starting at line 664) so that the three post-breakdown guard calls — `_enforce_module_import_order(state["breakdown"]["phases"], state["project_dir"])` (line 695), `_enforce_file_size_ceiling(state["breakdown"]["phases"], state["project_dir"])` (line 700), and `_enforce_no_raw_edits_to_encrypted_files(state["breakdown"]["phases"], state["project_dir"])` (line 705) — only run when this call is doing fresh chunk-to-phase assembly, not when re-entered to resume/dispatch an already-fully-populated `state["breakdown"]`. Implementation: capture `already_done = len(state["breakdown"]["phases"])` (existing line 675) *before* the `for i, chunk in enumerate(chunks):` loop as already happens, and additionally capture `chunks = _split_plan_by_phase(state["plan_text"])` length (existing line 674) to compute `newly_broken_down = already_done < len(chunks)` prior to entering the loop. Wrap the three guard-call blocks (lines 695-709) plus their `save_run(state)` at line 710 in `if newly_broken_down:` so they execute only on a call where the loop actually appended at least one new phase this invocation (i.e., genuine initial-breakdown progress, including a multi-call partial initial breakdown), and are skipped entirely when `breakdown_plan()` is re-entered purely to resume dispatch of a state whose `phases` list already equals `len(chunks)` at entry (the AGENTS.md-size-ceiling-blocks-resume incident case). Do not change any other behavior of `breakdown_plan()` (the empty-plan/zero-items check at lines 723-734 and the final `return {"status": "ok"}` must still run unconditionally on every call). Add a comment directly above the new `if newly_broken_down:` gate explaining why (references CARRYOVER.md item #1: a later, unrelated item's target drifting past a guard's threshold between initial breakdown and a resume must not retroactively block that resume). Verify: `python3 -m py_compile scripts/dispatcher.py`
   - [ ] In `tests/test_file_size_ceiling_and_oversize_escalation.py` (or a new test file `tests/test_breakdown_resume_guard_skip.py` if that fits better alongside the existing guard test files — match this repo's existing per-guard test file convention, one file per guard behavior), add a regression test that: (a) builds a `state` dict with `state["breakdown"]["phases"]` already fully populated (i.e. `len(phases) == len(_split_plan_by_phase(state["plan_text"]))`) where one phase's item targets a file that is currently on disk *above* `TIER4_MAX_CONTEXT_CHARS` (simulating the AGENTS.md-grew-too-big-between-breakdown-and-resume incident), (b) calls `breakdown_plan(state)` on this already-broken-down state, and (c) asserts the call returns `{"status": "ok"}` (not blocked by `_enforce_file_size_ceiling`) because no fresh chunk-to-phase assembly happened this call. Also add/keep a second test asserting the guard *does* still fire during a genuine fresh initial breakdown (i.e. `state["breakdown"]` starts as `None` or partially populated, oversized target present) so the fix doesn't silently disable the guard altogether. Verify: `PYTHONPATH=. python3 -m unittest tests.test_branch_features tests.test_file_size_ceiling_and_oversize_escalation tests.test_encrypted_file_edit_guard tests.test_dispatcher_test_context_guard -v`

2. Phase 2: Teach the planner correct `sops` syntax for this box's installed version
   - [ ] In `scripts/planner.py`, edit the `SYSTEM_PROMPT` string constant (starts at line 40, `SYSTEM_PROMPT = (`) to append a new paragraph stating: this box's installed `sops` is version 3.8.1 (confirmed via `sops --version`), which has NO `set` subcommand — `sops --help`'s COMMANDS list is only `exec-env`/`exec-file`/`publish`/`keyservice`/`groups`/`updatekeys`/`help`. Any plan step that edits a secrets file in place MUST use the `--set` flag on the default (edit-mode) invocation, never the `sops set FILE key value` subcommand form. Include the exact working syntax as a concrete example in the added text: for a nested key, `sops --set '["key"][0] "value"' FILE`; for a top-level key, `sops --set '["key"] "value"' FILE`. State explicitly in the added text: "Never generate `sops set FILE key value` — that subcommand does not exist on this box's sops 3.8.1." Keep the addition as a distinct paragraph appended to the existing prompt text (do not restructure or shorten the existing paragraphs) so the diff is minimal and reviewable. Verify: `python3 -m py_compile scripts/planner.py`
   - [ ] Confirm no test currently asserts on the literal `SYSTEM_PROMPT` string contents (this is a prompt-text-only change, not application logic, so no new test is required) by checking: `grep -rn "SYSTEM_PROMPT" tests/`. If any test does assert against `SYSTEM_PROMPT` contents, update that assertion to account for the new paragraph in the same step. Verify: `PYTHONPATH=. python3 -m unittest tests.test_branch_features tests.test_file_size_ceiling_and_oversize_escalation tests.test_encrypted_file_edit_guard tests.test_dispatcher_test_context_guard -v`

3. Phase 3: Deduplicate the shared `TIER4_MAX_CONTEXT_CHARS`/`MAX_WRITE_CHARS` constant
   - [ ] Create new file `scripts/tier4_context.py` containing only: a short module docstring explaining its purpose (single source of truth for the Tier 4 context-size ceiling, shared by `scripts/dispatcher.py` and `scripts/content_guard.py`, split out to avoid the circular import `dispatcher -> tier4_worker -> content_guard -> dispatcher`), and the constant definition `TIER4_MAX_CONTEXT_CHARS = 24576 * 3  # tier_4_worker num_ctx=24576 tokens (config/tiers.yaml) * 3 chars/token conservative floor -- see scripts/tier4_worker.py's call_ollama() options={"num_ctx": 24576}, the source of truth` (move this exact comment verbatim from `scripts/dispatcher.py` line 416). No other code, no imports beyond what's needed (none required). Verify: `python3 -m py_compile scripts/tier4_context.py`
   - [ ] Edit `scripts/dispatcher.py`: remove the local definition of `TIER4_MAX_CONTEXT_CHARS = 24576 * 3` at line 416 (and its trailing comment) and instead add `from scripts.tier4_context import TIER4_MAX_CONTEXT_CHARS` to the existing import block (alongside the other `from scripts...` imports at lines 33-39), keeping `TIER4_MAX_CONTEXT_CHARS` importable from `scripts.dispatcher` for backward compatibility (plain `from scripts.tier4_context import TIER4_MAX_CONTEXT_CHARS` at module level makes it re-exported automatically — do not use a private-name alias). All existing usages at lines 480 and 487 of `scripts/dispatcher.py` must keep working unchanged. Verify: `python3 -m py_compile scripts/dispatcher.py`
   - [ ] Edit `scripts/content_guard.py`: remove the local definition `MAX_WRITE_CHARS = 24576 * 3` at line 47 and its cross-referencing comment at line 42 (`# Kept in sync by hand with scripts.dispatcher.TIER4_MAX_CONTEXT_CHARS (not ...`), replacing the import with `from scripts.tier4_context import TIER4_MAX_CONTEXT_CHARS as MAX_WRITE_CHARS` added to the existing import block (after line 27's `from scripts.tri_logging import get_logger`), so `MAX_WRITE_CHARS` remains the name used at lines 61 and 67 unchanged. Update the removed comment to instead note (as a one-line comment above the import) that this now shares its value with `scripts.dispatcher.TIER4_MAX_CONTEXT_CHARS` via `scripts/tier4_context.py`. Verify: `python3 -m py_compile scripts/content_guard.py`
   - [ ] Check `tests/test_file_size_ceiling_and_oversize_escalation.py` line 23's `from scripts.dispatcher import (... TIER4_MAX_CONTEXT_CHARS)` and any other test file matched by `grep -rln "TIER4_MAX_CONTEXT_CHARS\|MAX_WRITE_CHARS" tests/` — since both names remain importable from their original modules (`scripts.dispatcher` and `scripts.content_guard` respectively) via re-export, no test import path should need to change; if any test imports directly from a now-removed local definition path, update it to import from `scripts.tier4_context` instead. Verify: `PYTHONPATH=. python3 -m unittest tests.test_branch_features tests.test_file_size_ceiling_and_oversize_escalation tests.test_encrypted_file_edit_guard tests.test_dispatcher_test_context_guard -v`

4. Phase 4: Full verification and docs
   - [ ] Run `python3 -m py_compile scripts/dispatcher.py scripts/content_guard.py scripts/tier4_context.py scripts/planner.py` and confirm zero errors/output.
   - [ ] Run `PYTHONPATH=. python3 -m unittest tests.test_branch_features tests.test_file_size_ceiling_and_oversize_escalation tests.test_encrypted_file_edit_guard tests.test_dispatcher_test_context_guard -v` and confirm all tests pass (no `SKIPPED`, no failures/errors).
   - [ ] Update `AGENTS.md` at the repo root: add `scripts/tier4_context.py` to its living file/dir index (one line describing it as the shared Tier-4 context-size-ceiling constant module, imported by `scripts/dispatcher.py` and `scripts/content_guard.py`), and note the `breakdown_plan()` resume-guard behavior change (post-breakdown guards now run only on fresh chunk assembly, not on resume of an already-populated breakdown) in whatever section of `AGENTS.md` documents `breakdown_plan()`/dispatcher guard behavior. Keep the addition concise per this repo's doc-hygiene convention (AGENTS.md must stay well under the 73,728-char Tier 4 size ceiling — check current size first with `wc -c AGENTS.md`).
