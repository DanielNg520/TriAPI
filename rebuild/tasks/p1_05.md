### TASK-P1-05: Ground-Truth Task Verification Entrypoint

File: TriAPI/rebuild/scripts/verify.py (append; the following already exist in this file — use them, do not redefine):
```python
def parse_test_output(output: str) -> dict: ...
def run_test_command(command, cwd=None, timeout=120) -> dict: ...  # {"passed","returncode","counts","zero_executed","stdout","stderr","error_message"}
def verify_file_content(file_path, expected_content=None, search_replace_blocks=None) -> dict: ...  # {"passed","file_exists","diff","violations"}
def verify_scope_boundaries(before_content, after_content, allowed_function_name=None, allowed_line_range=None) -> dict: ...  # {"passed","allowed_range","modified_lines","violations","diff"}
```

Signature to add:
```python
def verify_task(
    file_path: str | None = None,
    before_content: str | None = None,
    expected_content: str | None = None,
    search_replace_blocks: list[tuple[str, str]] | None = None,
    allowed_function_name: str | None = None,
    allowed_line_range: tuple[int, int] | None = None,
    test_cmd: str | list[str] | None = None,
    cwd: str | None = None,
    timeout: int = 120,
) -> dict:
```

Behavior:
- checks_run = 0 (count how many of the 3 checks below actually execute)
- evidence = {} — will hold "test_run", "content_check", "scope_check" keys, each only present if that check ran.
- all_passed = True
- If test_cmd is not None: checks_run += 1; evidence["test_run"] = run_test_command(test_cmd, cwd=cwd, timeout=timeout); if not evidence["test_run"]["passed"]: all_passed = False
- If file_path is not None and (expected_content is not None or search_replace_blocks is not None): checks_run += 1; evidence["content_check"] = verify_file_content(file_path, expected_content, search_replace_blocks); if not evidence["content_check"]["passed"]: all_passed = False
- If before_content is not None and (allowed_function_name is not None or allowed_line_range is not None): read after_content from file_path (must be given too) via open(file_path, encoding="utf-8").read(); checks_run += 1; evidence["scope_check"] = verify_scope_boundaries(before_content, after_content, allowed_function_name, allowed_line_range); if not evidence["scope_check"]["passed"]: all_passed = False
- If checks_run == 0: passed = False, summary = "Verification failed: no checks were supplied"
- Else: passed = all_passed; build summary as a short human-readable string listing which checks ran and their pass/fail (e.g. "tests: 5 passed" or "tests: FAILED (Zero tests executed)"; "file content: matched" or "file content: MISMATCH"; "scope: intact" or "scope: VIOLATED"), joined with "; ". Prefix with "All checks passed" if passed else "Verification failed: " + the summary detail.
- Do not catch any exceptions raised by the called functions — let them propagate.

Return dict with exactly: "passed" (bool), "summary" (str), "evidence" (dict).

Scope:
- In scope: orchestration only, calling the three existing functions and building the result.
- Out of scope: no new verification logic, no retries, no auto-fix, no exception handling.
