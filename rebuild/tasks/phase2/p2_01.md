### TASK-P2-01: Atomic File Change Applicator

File: TriAPI/rebuild/scripts/dispatch.py (new file; `from pathlib import Path` needed)

Signature:
```python
def apply_change(
    file_path: str,
    new_content: str | None = None,
    search_replace_blocks: list[tuple[str, str]] | None = None,
) -> str:
```

Behavior:
- Raise ValueError if neither or both of new_content/search_replace_blocks are given (exactly one must be provided).
- Read file_path as UTF-8. If it doesn't exist, raise FileNotFoundError (let Path.read_text's natural error propagate, or raise explicitly).
- Snapshot original = the content just read.
- If new_content is given: the content to write is new_content.
- If search_replace_blocks is given: for each (search_text, replace_text) pair, count occurrences of search_text in the CURRENT snapshot (original, before any replacement in this call) — if count != 1, raise ValueError(f"search_text must appear exactly once, found {count}: {search_text!r}") BEFORE writing anything to disk. Only after all blocks validate, apply them in order to build the final content (str.replace(search, replace, 1) for each, applied sequentially against the running result).
- Write the final content to file_path (UTF-8), overwriting.
- Return original (the pre-modification content).

Scope:
- In scope: read/validate/write as described.
- Out of scope: do not call verify_task, do not restore on failure, no subprocess/git calls.
