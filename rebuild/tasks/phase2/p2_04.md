### TASK-P2-04: Dispatcher Unit Test Suite

File: TriAPI/rebuild/tests/test_dispatch.py (new file)

Import from `scripts.dispatch`: apply_change, restore_file, dispatch_task. Use `unittest.mock.patch` to mock `scripts.dispatch.verify_task` (it's imported into that module's namespace, so patch it there: `unittest.mock.patch("scripts.dispatch.verify_task")`) for the dispatch_task tests. Use tmp_path for real files.

Tests:
1. test_apply_change_new_content: write a tmp_path file with "old\n", call apply_change(path, new_content="new\n"), assert returned original == "old\n" and file now contains "new\n".
2. test_apply_change_search_replace_blocks: write a tmp_path file "aaa bbb ccc\n", call apply_change(path, search_replace_blocks=[("aaa","xxx"),("ccc","zzz")]), assert file now "xxx bbb zzz\n".
3. test_apply_change_missing_or_duplicate_search_blocks: write a tmp_path file "aaa bbb aaa\n" (aaa appears twice). Call apply_change(path, search_replace_blocks=[("aaa","x")]) and assert it raises ValueError. Also test a file "bbb\n" (no "aaa") with search_replace_blocks=[("aaa","x")]) raises ValueError. In both cases assert the file content on disk is unchanged after the exception.
4. test_restore_file: write a tmp_path file "modified\n", call restore_file(path, "original\n"), assert file now contains "original\n".
5. test_dispatch_task_success: tmp_path file "old\n". Mock verify_task to return {"passed": True, "summary": "ok", "evidence": {}}. Call dispatch_task(path, new_content="new\n"). Assert result["passed"] is True, result["rolled_back"] is False, and file on disk still contains "new\n" (not rolled back).
6. test_dispatch_task_verification_failure_triggers_rollback: tmp_path file "old\n". Mock verify_task to return {"passed": False, "summary": "bad", "evidence": {}}. Call dispatch_task(path, new_content="new\n"). Assert result["passed"] is False, result["rolled_back"] is True, and file on disk is back to "old\n".
7. test_dispatch_task_apply_error_returns_failure: tmp_path file "aaa aaa\n" (ambiguous). Call dispatch_task(path, search_replace_blocks=[("aaa","x")]) with verify_task mocked (should never be called). Assert result["passed"] is False, result["rolled_back"] is False, "Dispatch error" in result["summary"], and file on disk unchanged.

Scope:
- In scope: pytest tests as described, tmp_path, mock.patch for verify_task only.
- Out of scope: do not run real external test suites, do not modify scripts/dispatch.py or scripts/verify.py.
