### TASK-P2-02: File Content Restorer

File: TriAPI/rebuild/scripts/dispatch.py (append; `Path` already imported)

Signature:
```python
def restore_file(
    file_path: str,
    original_content: str,
) -> None:
```

Behavior: overwrite file_path with original_content, UTF-8 encoding. Nothing else.

Scope:
- In scope: the write only.
- Out of scope: no verification, no exception handling, no git.
