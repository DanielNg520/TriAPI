### TASK-P3-04: Cost Tracking Unit Test Suite

File: TriAPI/rebuild/tests/test_cost.py (new file)

Import from `scripts.cost`: calculate_cost, log_cost, read_cost_summary.

Tests (use tmp_path for all log/config files — never touch the real TriAPI/config/tiers.yaml or TriAPI/rebuild/logs/cost_log.jsonl):

1. test_calculate_cost_basic: write a tmp_path yaml file with:
```yaml
deepseek_reference_pricing:
  cache_miss_per_mtok_usd: 0.14
  output_per_mtok_usd: 0.28
```
   calculate_cost(1_000_000, 1_000_000, config_path=str(that file)) == pytest.approx(0.14 + 0.28)

2. test_calculate_cost_output_rate_null_fallback: same but output_per_mtok_usd: null in the yaml. calculate_cost(1_000_000, 1_000_000, config_path=...) == pytest.approx(0.14 + 0.14) (falls back to cache_miss rate).

3. test_calculate_cost_missing_config: calculate_cost(1, 1, config_path=str(tmp_path / "nope.yaml")) raises FileNotFoundError.

4. test_log_cost_creates_file_and_appends: use log_path = tmp_path / "sub" / "cost_log.jsonl" (subdir doesn't exist yet). Call log_cost("task-a", 10, 20, log_path=str(log_path)). Assert the file now exists, contains exactly one line, that line is valid JSON with task_id "task-a", input_tokens 10, output_tokens 20, and a "timestamp" string field. Call log_cost again with a second task; assert file now has 2 lines.

5. test_read_cost_summary_missing_and_empty: read_cost_summary(log_path=str(tmp_path / "nope.jsonl")) returns all zeros / entry_count 0. Also create an empty file at another tmp_path location and assert the same zeroed result.

6. test_read_cost_summary_multiple_entries: write a tmp_path pricing yaml (cache_miss_per_mtok_usd: 0.14, output_per_mtok_usd: 0.28) and a tmp_path jsonl log with 2 entries (input_tokens 100/200, output_tokens 50/50). Call read_cost_summary(log_path=..., config_path=...). Assert total_input_tokens == 300, total_output_tokens == 100, entry_count == 2, and total_cost_usd == pytest.approx(calculate_cost(300, 100, config_path=that yaml)).

Scope:
- In scope: pytest tests as described, tmp_path only.
- Out of scope: do not touch real TriAPI/config/tiers.yaml or TriAPI/rebuild/logs/cost_log.jsonl, no mocking.
