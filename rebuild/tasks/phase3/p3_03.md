### TASK-P3-03: Cost Log Summary Aggregator

File: TriAPI/rebuild/scripts/cost.py (append; calculate_cost already defined in this file — use it, do not redefine; json/Path already imported)

Signature:
```python
def read_cost_summary(
    log_path: str | None = None,
    config_path: str | None = None,
) -> dict:
```

Behavior:
- Default log_path (if None): Path(__file__).resolve().parents[1] / "logs" / "cost_log.jsonl"
- If the file doesn't exist, or exists but is empty (0 bytes / no non-blank lines): return {"total_input_tokens": 0, "total_output_tokens": 0, "total_cost_usd": 0.0, "entry_count": 0}
- Otherwise: read the file line by line, json.loads each non-blank line, sum "input_tokens" and "output_tokens" across all entries, count entries.
- total_cost_usd = calculate_cost(total_input_tokens, total_output_tokens, config_path=config_path)
- Return {"total_input_tokens": total_input_tokens, "total_output_tokens": total_output_tokens, "total_cost_usd": total_cost_usd, "entry_count": entry_count}

Scope:
- In scope: read+parse jsonl, sum, call calculate_cost.
- Out of scope: no printing/formatting, no deleting/archiving entries.
