### TASK-P3-01: Reference Pricing & Cost Calculator

File: TriAPI/rebuild/scripts/cost.py (new file; needs `yaml`, `from pathlib import Path`)

Signature:
```python
def calculate_cost(
    input_tokens: int,
    output_tokens: int,
    config_path: str | None = None,
) -> float:
```

Behavior:
- Default config_path (if None): Path(__file__).resolve().parents[2] / "config" / "tiers.yaml" (rebuild/scripts/cost.py -> rebuild -> TriAPI -> config/tiers.yaml).
- Load YAML via yaml.safe_load. If file doesn't exist, let FileNotFoundError propagate naturally (from open()/Path.read_text()).
- Extract config["deepseek_reference_pricing"] (let KeyError propagate if missing).
- cache_miss_rate = pricing["cache_miss_per_mtok_usd"] (let KeyError propagate if missing).
- output_rate = pricing.get("output_per_mtok_usd") — if it's None or missing, fall back to cache_miss_rate.
- Return (input_tokens / 1_000_000 * cache_miss_rate) + (output_tokens / 1_000_000 * output_rate)

Scope:
- In scope: read yaml, compute float cost as described.
- Out of scope: no hardcoded pricing fallback numbers, no disk logging, no network calls.
