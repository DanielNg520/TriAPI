### TASK-P1-06: Verification Layer Test Suite

File: TriAPI/rebuild/tests/test_verify.py (new file)

Import from `scripts.verify`: parse_test_output, run_test_command, verify_file_content, verify_scope_boundaries, verify_task. Assume tests run with rebuild/ as the working directory / on sys.path (`from scripts.verify import ...` works).

Write pytest test functions (use tmp_path fixture for file operations, no mocking of subprocess — run real `python3 -c "..."` or write tiny real test files and invoke real pytest as a subprocess, since these are cheap and this module's whole point is ground-truth over mocks):

1. test_parse_test_output_pytest_style: parse_test_output("5 passed, 1 skipped in 0.12s") == {"passed":5,"failed":0,"errors":0,"skipped":1,"total_executed":5}
2. test_parse_test_output_unittest_style: parse_test_output("Ran 6 tests in 0.05s\n\nFAILED (failures=1, errors=1, skipped=1)") == {"passed":3,"failed":1,"errors":1,"skipped":1,"total_executed":5}
3. test_run_test_command_zero_executed_fails: write a tmp_path test file containing only a pytest.skip()'d test, run it via run_test_command(["python3","-m","pytest","-v",str(file)]), assert result["passed"] is False and result["zero_executed"] is True
4. test_run_test_command_real_pass: write a tmp_path test file with one passing test, run it, assert result["passed"] is True
5. test_verify_file_content_match_and_mismatch: write a tmp_path file "hello\n"; verify_file_content(path, expected_content="hello\n") passed True; verify_file_content(path, expected_content="bye\n") passed False with "Content mismatch" in violations
6. test_verify_file_content_missing_file: verify_file_content(str(tmp_path / "nope.txt")) -> file_exists False, passed False
7. test_verify_scope_boundaries_in_and_out_of_scope: use the before/after Python snippets with two functions foo/bar (same as manual check above), assert an edit inside "bar" passes and an edit inside "foo" (when only "bar" is allowed) fails with a violation mentioning "outside allowed span"
8. test_verify_task_no_checks_fails: verify_task() -> passed False, "no checks" in summary.lower()
9. test_verify_task_composes_test_run: verify_task(test_cmd=[...]) with a real passing test file -> passed True

Scope:
- In scope: pytest test functions only, using tmp_path and real subprocess calls (python3 -m pytest as a child process is fine).
- Out of scope: do not modify scripts/verify.py, no network access, no mocking subprocess.
