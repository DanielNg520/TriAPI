### TASK-P1-01: Test Output Parser
Goal: Parse stdout/stderr from pytest/unittest into exact counts of passed, failed, errors, skipped.

File: TriAPI/rebuild/scripts/verify.py
Signature:
```python
def parse_test_output(output: str) -> dict[str, int]:
```

Input: `output: str` — combined stdout+stderr text (e.g. pytest summary line `5 passed, 1 skipped in 0.12s`, or unittest `Ran 6 tests in 0.05s ... OK (skipped=1)`).

Output: dict with exactly these keys:
- "passed": int
- "failed": int
- "errors": int
- "skipped": int
- "total_executed": passed + failed + errors (excludes skipped)

Scope:
- In scope: pure regex parsing of pytest and unittest terminal summary formats; missing counts default to 0.
- Out of scope: no subprocess execution, no file reads, no pass/fail determination — parsing only.

Needed imports only (e.g. `re`). No other functions.
