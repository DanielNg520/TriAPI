### TASK-P3B-05: Code-Block Extractor Unit Test Suite

File: TriAPI/rebuild/tests/test_llm_client.py (new file)

Import: `from scripts.llm_client import extract_code_block`

Note the real signature: `extract_code_block(response: str, language: str = "python") -> str`. It returns a plain `str` (not an object) — assert on the returned string directly, e.g. `assert extract_code_block(resp) == "expected"`.

Tests:
1. test_extract_code_block_exact_language: response = "some text\n```python\ndef f():\n    return 1\n```\nmore text". extract_code_block(response) == "def f():\n    return 1"
2. test_extract_code_block_bare_fallback: response = "text\n```\nkey: value\n```\n". extract_code_block(response, language="yaml") == "key: value"
3. test_extract_code_block_preserves_content_whitespace: response with a block body containing a blank line and trailing spaces on one line, e.g. "```python\n    a = 1\n\n    b = 2   \n```". Assert the returned string equals exactly "    a = 1\n\n    b = 2   " (indentation, blank line, and trailing spaces all preserved).
4. test_extract_code_block_first_block_priority: response with two ```python blocks. extract_code_block(response) returns the content of the FIRST one only.
5. test_extract_code_block_custom_language: response = "text\n```yaml\nkey: value\n```\n". extract_code_block(response, language="yaml") == "key: value"
6. test_extract_code_block_missing_raises_value_error: extract_code_block("no code blocks here at all") raises ValueError.

Scope: pure pytest, no mocking, no network, no execing the extracted code.
