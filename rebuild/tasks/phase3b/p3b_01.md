### TASK-P3B-01: Cumulative Spend Budget Checker

File: TriAPI/rebuild/scripts/cost.py (append; read_cost_summary already defined in this file — use it, do not redefine)

Signature:
```python
def check_budget(
    limit_usd: float,
    log_path: str | None = None,
    config_path: str | None = None,
) -> dict:
```

Behavior:
- summary = read_cost_summary(log_path=log_path, config_path=config_path)
- total = summary["total_cost_usd"]
- Return {"under_limit": total <= limit_usd, "total_cost_usd": total, "limit_usd": limit_usd, "remaining_usd": limit_usd - total}

Scope:
- In scope: call read_cost_summary, compare, return dict as described.
- Out of scope: no raising on over-budget, no file writes, no reading model_config.yaml directly, no printing.
