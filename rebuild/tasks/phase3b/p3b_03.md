### TASK-P3B-03: Fenced Code-Block Extractor

File: TriAPI/rebuild/scripts/llm_client.py (append; needs `import re`)

Signature:
```python
def extract_code_block(
    response: str,
    language: str = "python",
) -> str:
```

Behavior:
- Search for the first block matching: ```` ```{language}\n(.*?)\n``` ```` (re.DOTALL) in response. If found, return the captured group verbatim (no strip beyond what the regex itself excludes — the fence lines and their newlines).
- If not found, search for the first bare block: ```` ```\n(.*?)\n``` ```` (re.DOTALL, this will also match ```` ```{language} ```` blocks again if the first search failed for some other reason — that's fine, just try language-specific first, then bare, first match wins for each).
- If neither is found, raise ValueError("no fenced code block found").
- Do not strip/dedent the captured content itself. Do not execute or parse it.

Scope:
- In scope: regex-based extraction exactly as described.
- Out of scope: no code execution, no modifying the input string, no stripping internal whitespace.
