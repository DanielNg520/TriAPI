### TASK-P3B-04: Spend Cap Unit Test Suite

File: TriAPI/rebuild/tests/test_cost.py (append)

Import check_budget from scripts.cost (already imports calculate_cost, log_cost, read_cost_summary — add check_budget to that import line).

Tests (tmp_path only, never touch real logs/config):
1. test_check_budget_under_limit: write a tmp_path pricing yaml (cache_miss_per_mtok_usd: 0.14, output_per_mtok_usd: 0.14) and a tmp_path jsonl log with one small entry (e.g. input 100, output 100). check_budget(5.0, log_path=..., config_path=...) -> under_limit True, total_cost_usd small positive, remaining_usd close to 5.0 (positive).
2. test_check_budget_over_limit: same style but log entries totaling a cost above the limit (e.g. limit=0.0001, entry input=1_000_000, output=1_000_000). under_limit False, remaining_usd negative.
3. test_check_budget_exact_limit: construct a log/pricing combo where total_cost_usd == limit_usd exactly (compute the limit from calculate_cost given the log's tokens, using the same config), assert under_limit True and remaining_usd == pytest.approx(0.0).
4. test_check_budget_missing_and_empty_log: check_budget(5.0, log_path=str(tmp_path / "nope.jsonl")) -> under_limit True, total_cost_usd 0.0, remaining_usd == pytest.approx(5.0).

Scope: pytest + tmp_path only, no mocking, no touching real production files.
