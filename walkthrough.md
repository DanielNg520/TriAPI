# Replace Gemini API with Nemotron
- Instructed TriAPI to dispatch a plan that automatically edits `config/tiers.yaml` to update `tier_2_manager` to Nemotron's OpenRouter free model `nvidia/nemotron-3-ultra-550b-a55b:free`.
- Updated `scripts/tier2_escalate.py` to correctly parse OpenAI API syntax matching OpenRouter endpoints instead of Gemini schema.
- Replaced `gemini_fallback` with an iterative model fallback logic that can accept custom slugs through CLI via `--model`.
- Orchestrated another headless pipeline dispatch to update `orchestrator.py` and `scripts/tierX_escalate.py`/`tier4_worker.py` dynamically allowing them to read and forward `model` slugs as arguments (e.g. `--tier1-model`, `--tier2-model`, etc.).
- Dispatched an automated plan to dynamically support both Claude CLI (`claude -p`) and OpenRouter inside `scripts/tier1_escalate.py`. If `tier_1_planner` endpoint is set to `"cli"`, it reverts to `subprocess.run(["claude", "-p", ...])` and logs subscription costs. Otherwise, it executes a REST call against OpenRouter.
- Cancelled the initial "fail-fast and fail hard" refactor based on new user directive.
- Instructed TriAPI to implement a globally configurable LLM client (`scripts/llm_client.py`) that abstracts the execution (CLI, OpenRouter, Gemini natively) and gracefully falls back on failure:
  - Tiers 1-3 Fallback: Checks off-peak limits, falls back to DeepSeek natively. If still failing or during peak limits, falls back natively to Gemini Flash.
  - Tier 4 Fallback: Local Ollama daemon.
- The pipeline is currently wiring this new client into `tier1_escalate.py`, `tier2_escalate.py`, `tier3_escalate.py`, and `tier4_worker.py`.
- Finished wiring `llm_client.execute_llm()` into the pipeline tiers. The `fallback_chain` loops have been completely removed from `tiers.yaml`, and all Python scripts are dynamically resolving their API keys, primary providers, and intelligently delegating fallbacks.
