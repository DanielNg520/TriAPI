# Carryover: SemAI Phase 8 dispatched, audited, false-successes found+fixed (2026-09-05)

**Session summary.** Dispatched SemAI's queued Phase 8 ('full agentic mode, same capacity on every direct-prompt channel', the plan.md capability track's own Phase 8 -- now done, separate from the already-complete 0-8 consolidation-phase numbering) via 'triapi plan'/'dispatch' (run 20260905-001032-949898, commit b8e2638). All 7 items reported success. Post-dispatch supervisor audit (per standing instruction to always audit after every phase) read the actual diffs rather than trusting the reports, and found two real false-successes: Telegram's new agent-routing code called ApprovalStore.create() with a signature that does not match the real one and hardcoded kind='agent' (ProposedAction has no .kind field), which would crash or silently fail to resolve any approval in production -- masked by a FakeApprovalStore test double with a mismatched, over-permissive **kwargs signature; and the CLI REPL item was a no-op (only an unused settings parameter was added, repl() still unconditionally called the legacy dispatcher).

**Fix dispatch** (run 20260905-012833-54d9d7): root-caused via AgentResult.proposals changing shape from list[ProposedAction] to list[tuple[str, ProposedAction]] so the correct registered kind flows to every approval_store.create() call site. This also exposed and fixed a third, previously-undetected instance of the same wrong-kind bug in daemon.py's agent-fallback branch. Two escalations were trivial mechanical fixes the tiers got stuck on and I hand-patched directly (a stale test assertion string, and a frozen-pydantic-Settings mutation in a test) -- both zero-judgment, already covered by existing tests, not new hand-written features. After the dispatch's own full-suite verify passed, I independently re-ran the full suite myself and found two MORE regressions the tiers had missed entirely (tests/test_cli_adapter.py never awaiting the now-async repl(), and tests/test_semai_agent.py asserting the old bare-ProposedAction shape) -- fixed both by hand (same zero-judgment bar) before resuming. Both SemAI commits (b8e2638, 8bfa802) pushed to the migration-clean-up branch.

**One dispatch item needed manual closeout**, not a hand-written fix: the final doc-update step told a tier to invoke 'uv run python scripts/librarian_escalate.py ...' from TriAPI's own root, which crashed (TriAPI has no pyproject.toml, so uv run builds a bare env missing requests) after all tiers failed on it. I ran the correct invocation directly (plain python3, correct flags) and confirmed by hand that the target file already carried the correct content from an earlier partial attempt, then state-patched that run item's result (replaced, not appended, per standing convention) to success and set the run status to completed, since nothing further needed doing.

**TriAPI self-fix** (commit f4a5317 on main, not yet pushed): root-caused the uv-run crash generically -- scripts/librarian_escalate.py's run_command() now strips a leading 'uv run' before any command invoking a TriAPI scripts/*.py file, so a future plan/breakdown/verify_cmd string making the same mistake self-corrects instead of crashing. Verified: TriAPI's own tests/test_tier5_librarian.py (27 tests + 8 subtests) still green.

**Also found+cleared**: two stale knowledge/TECH_DEBT.md entries from mid-dispatch tier-escalation churn against SemAI's cli.py, confirmed resolved by direct SHA-256 hash comparison against the current, fully-tested file content, then manually removed (triapi tech-debt --project-dir flagged both 'needs manual review', which this was).

**Repeated low-memory dispatch kills, not a TriAPI bug.** Several background 'triapi dispatch' invocations were killed by the harness's low-memory guard, including once with 28GB available -- not a real resource shortage on TriAPI's end. Foreground retries made progress past each kill; one large external memory-freeing event (an unrelated long-running process on the box, not started by this session, most likely 'agy' finishing) freed roughly 19GB partway through. Nothing to fix on TriAPI's side.

**Housekeeping, still in this state:** at the user's explicit request, oh-my-llama.service, oh-my-llama-telegram.service, and openclaw.service were stopped (miki-sorter.service deliberately left running) to free memory during the dispatch churn. They have NOT been restarted -- ask the user before restarting, since SemAI's live daemon/Telegram bot being down is a deliberate current state, not an incident.

**On-hold items, re-carried (do not let these silently drop again):**
- VIRTUAL_CODEBASE_PLAN.md -- Tiered Planner-Materializer design for large-file Tier 4 edits. User wants to work on this together personally, never start solo.
- docs/TUI_plan.md -- confirmed 'triapi tui' subcommand spec, blocker cleared long ago, never dispatched. Open design questions need the user before planning.

Nothing else in flight. Full SemAI suite green, full TriAPI librarian suite green, both repos' working trees clean (SemAI pushed; TriAPI committed locally, not pushed).
