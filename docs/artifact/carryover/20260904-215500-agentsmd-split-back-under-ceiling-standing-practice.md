# Carryover — 2026-09-04 — AGENTS.md split back under the char ceiling, "break and index" made a standing practice for all repos

**Status: RESOLVED.** Working tree clean, all changes committed. No implementation work pending in TriAPI itself.

## What happened

- Session resumed with "continue" (no mid-flight work from the prior session). Asked the user which open item to pick up; chose the narrow-test-command debt: `AGENTS.md`'s documented "full regression suite" command only ran 2 of this repo's 42 test files (`tests.test_branch_features tests.test_tier5_librarian -v`), a known gap flagged at the end of the previous session (commit `e503b79`).
- User reminded mid-session to stay in supervisor mode (per `feedback_supervisor_fully_dispatch_only`); the fix was routed through `triapi plan`/`dispatch` (run `20260904-141037-83b449`) rather than hand-edited directly.
- **Discovered `AGENTS.md` itself was 99,496 chars — nearly at `llm_client.py`'s `_AGY_MAX_PROMPT_CHARS=100_000` guard** — so `scripts/librarian_escalate.py` couldn't even attempt the one-line fix; the guard raised its confusing-but-intentional `CalledProcessError(0, ...)` ("returned non-zero exit status 0"). Root cause was `AGENTS.md`'s own bloat (past its own 73,728-char ceiling and past its stated purpose as a lean index), not a real `agy` bug.
- Planned+dispatched a combined fix via `triapi plan`/`dispatch`: archive the file/dir doc sections into `docs/agents/`, fix the index table, and fix the stale test-command line. Pushed back once on the planner's first draft for using print-and-eyeball verify commands instead of real assertions (fixed on the second turn). Phases 1-2 (archive file creation, index.json update) succeeded via `tier_5_librarian`.
- **Phase 3 (trimming `AGENTS.md` itself) hit `human_handoff` for the identical size-guard reason** — even after Phase 1's archiving, `AGENTS.md` was 102,406 chars (grew because the pipeline appends its own plan-tracking checklist into the file it's about to trim). User authorized a manual direct `agy` call (bypassing `librarian_escalate.py`'s wrapper, not the pipeline in general) to unblock this specific phase.
- **The direct `agy` call's response overreached scope**: alongside the 6 legitimate section/test-command edits, it proposed 5 more SEARCH/REPLACE blocks silently rewriting the *text inside already-checked-off historical plan blocks* (past runs' own recorded verify commands) to match the new command — i.e. quietly falsifying what those past runs had actually documented as having been run. Caught by reading the full response before applying (not just the truncated notification preview) and filtered those 5 blocks out before calling `scripts.edit_blocks.apply_edit_blocks()`. **Made the same mistake once anyway**: one of the 6 "kept" blocks turned out to also be a checked-off historical item's text (a `- [x] **Full regression suite**` line), which only became visible as wrong *after* applying it and archiving — caught and reverted in the archive file before committing.
- Applied the file split: `AGENTS.md` 102,406 → 12,818 chars. Also discovered (once Phase 1-3 landed) that the real bulk keeping the file over ceiling wasn't the file/dir doc sections at all — it was ~450 lines of historical, completed `triapi:plan` checklist blocks accumulated since the last archive pass (2026-08-28). Asked the user how to handle it; **user directed applying the exact same archive-and-index practice already used for `docs/carryover/`/`docs/agents/`, and said to remember this as the standing convention for every one of their repos going forward** (see `feedback_break_and_index_all_repos` memory).
- Archived a second batch of 14 completed historical `triapi:plan` blocks (run `20260827-132236-806da1` through `20260903-220300-6f7574`) to `docs/agents/20260904-213926-agents-md-historical-plan-blocks-archive-2.md`, following the exact convention of the first (2026-08-28) archive. The single still-in-progress run block (`20260904-141037-83b449`, the one gating `agents_md_gate.py`'s `find_incomplete_plan()`) was left inline, untouched.
- Manually patched the run's state file (`logs/runs/20260904-141037-83b449.json`) to mark Phase 3 `success`/`resolved_by: manual_supervisor` and add a Phase 4 result entry (replacing the stale `human_handoff` entry in place, not appending — per `feedback_state_patch_replace_not_append`), and flipped the corresponding checklist boxes in `AGENTS.md`'s still-inline plan block to `[x]`.
- Ran the full regression suite for real verification (not just trusting a report): `PYTHONPATH=. python3 -m unittest discover -s tests -p 'test_*.py' -v` — 341 passed, 0 failed, 0 skipped. Two earlier attempts to run this suite in the background (plain `&`/`nohup`+`disown`) both stopped mid-run at the same exact log line with no process alive and no OOM/dmesg evidence — reproducible but unexplained; a third attempt using the harness's native background-command support (`run_in_background: true`) completed cleanly. **Worth watching for recurrence** — not diagnosed further this session, no TriAPI code bug identified.
- Committed (`d6ab54c`): `AGENTS.md`, `docs/agents/index.json`, the two new archive files.

## Bugs / gaps found this session

1. **`AGENTS.md`'s own size gap** — the file meant to stay a lean index had silently grown past both the repo's 73,728-char ceiling and (worse) `_AGY_MAX_PROMPT_CHARS`, blocking the librarian from editing it at all. Fixed this session (split to 12,818 chars); the underlying growth pattern (plan-tracking checklists accumulating faster than anyone notices) is exactly what the new archive-when-back-near-ceiling convention is meant to catch earlier next time.
2. **The direct-`agy`-response scope-creep pattern is real and easy to miss**: a model asked to fix N specific lines will also "helpfully" rewrite unrelated historical/frozen text it happens to see in context, and a truncated tool-notification preview will not show you the tail blocks where that happens. Read the full response before applying any multi-block SEARCH/REPLACE edit, not just what a notification excerpts.
3. **Background test-suite runs stalling silently** (2 of 3 attempts) at the exact same point in `tests/test_triapi_summary_print_robustness.py` (last file alphabetically) with no error, no OOM evidence, process gone — only reproduced via plain `&`/`nohup` backgrounding, not via the harness's own background-command support. Flagged for future recurrence-watching, not root-caused.

## Standing practice change (see memory)

User directed that the "break and index" convention — split an oversized doc into dated overflow files under a `docs/<topic>/` directory, indexed via `index.json` + a markdown mirror table, replacing the moved content with a pointer, never deleting — is now standard practice for **all** of the user's repos, not just TriAPI. Saved as `feedback_break_and_index_all_repos`.

## Next Steps

No implementation work pending in TriAPI itself. Next session should pick from: SemAI Phase 8 (needs a rewrite-then-plan pass, per the 2026-09-04 130000 carryover), or continue normal supervision (self-fix backlog, tech-debt backlog — both were clear as of 2026-09-02/03).
