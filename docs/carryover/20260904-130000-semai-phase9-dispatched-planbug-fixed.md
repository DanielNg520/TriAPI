# Carryover — 2026-09-04 — SemAI Phase 9 dispatched, `triapi plan` approval bug fixed

**Status: RESOLVED.** Pure supervision session against the SemAI target repo (no TriAPI feature work queued or requested) plus one real TriAPI bug found and fixed via self-fix. Working tree clean, all changes committed and pushed on both repos.

## What happened

- User asked to check the SemAI dispatch queue and have TriAPI process it. No literal queue file exists in SemAI; the two genuinely-queued items were Phase 8 (full agentic mode across channels) and Phase 9 (regression/doc reconciliation).
- Recovered a lost root-level `plan.md` (SemAI, deleted 2026-08-16 commit `2a1e974`, content never migrated) via `git show 2a1e974^:plan.md` and fixed the phantom `docs/plan.md` link references it left behind in SemAI's `AGENT_GUIDE.md`/`CARRYOVER.md`. Phase 8's recovered text is real but stale (cites the removed `ohmyllama/` tree and a Discord channel no longer in `src/semai/`) — queued in SemAI's own carryover for a future session, not started here per explicit user direction to do Phase 9 first.
- **Found and fixed a real TriAPI bug** (self-fix run `20260904-115032-017aeb`, commit `f130296`): `cmd_plan()` in `scripts/triapi.py` accepted `'approve'` on ANY planner turn, including a bare clarifying question with no checklist content. A piped `approve` (needed because `triapi plan` requires an interactive terminal) got consumed by the question-turn's own approval prompt, saving the question text as the run's `plan_text`, marking it `planned`, and appending it into the **target repo's** `AGENTS.md` as if it were a real plan. Fix: `cmd_plan` now requires `"- [ ] "` checklist markers in a turn before `'approve'` is honored; otherwise it prints a refusal and re-prompts for real feedback (piped-EOF safely aborts instead of silently approving garbage). Cleanup performed by hand (minor/mechanical, approved): reverted the bogus `AGENTS.md` block this bug wrote into SemAI, manually marked the broken run `20260904-114233-213429` `cancelled` in its own `logs/runs/*.json` (overwrite, not append).
- **Self-audit caught the self-fix dispatch's own verification was insufficient** (commit `e503b79`): the dispatch item verified its fix with only `tests.test_branch_features tests.test_tier5_librarian -v` (89/89 pass) — this repo's *documented* "full regression suite" command in `AGENTS.md`, which is itself dangerously narrow and skips 40 of 42 test files. A full `python3 -m unittest discover -s tests` run (341 tests) surfaced a real regression: `test_flagged_plan_double_approve_confirms` used a mocked turn text (`"ok"`) with no checklist markers, so the new gate added a third `input()` call its 2-item `side_effect` couldn't satisfy, raising an uncaught `StopIteration`. Fixed by isolating that test to the flagged-content double-confirm gate specifically (still-short-but-checklist-shaped text) and adding `tests/test_triapi_plan_approval_checklist_gate.py` with direct coverage for the new gate (blind-piped-approve abort, approve-then-cancel, approve-then-real-feedback continuing the loop). Full `unittest discover` suite: 341 passed, 0 failed, 0 skipped. **`AGENTS.md`'s documented test command being too narrow to catch a real regression is itself worth fixing** (either broaden the documented command or add a discover-based CI-style check) — not done this session, flagged here.
- Dispatched SemAI Phase 9 (`docs/README.md` reconciliation) — `triapi plan` (run `20260904-121955-a213fe`), this time answering the planner's real clarifying question (it had independently grepped `src/semai/` and found Calendar/browser-errand still live, Mail on the direct Gmail API not IMAP, no Drive-upload worker — corrected my own assumptions) rather than blind-approving.
- Dispatch hit `tier_5_librarian`'s `agy` call failing (`returned non-zero exit status 1`) twice in a row (`stopped_on_failure` → `human_handoff`). Reproduced by hand via a direct `agy` subprocess call with the identical prompt/argv — it succeeded on the third attempt (once with a wrong `FRESH`/no-change-needed verdict on a manual repro, a red flag worth remembering: don't trust a librarian "FRESH" verdict on an obviously-stale large file without checking the actual diff). Concluded transient (network/quota), not a reproducible TriAPI bug — no self-fix filed for this one, unlike the approval bug above.
- Separately, this session's dispatches repeatedly hit the harness's low-memory guard killing the backgrounded `triapi dispatch` process outright (not just the wrapper) — root cause was orphaned local Ollama models (5.3GB + 15GB) left loaded from earlier killed attempts, compounding with normal desktop memory pressure (Chrome, other sessions). Fixed by `ollama stop`-ing the orphaned models before retrying; not a TriAPI code bug, but worth remembering as a recovery step if dispatch keeps dying under `[killed]` for low memory: check `ollama ps` and `free -h` before assuming it's the plan itself.
- Third dispatch attempt succeeded (`resolved_by=tier_5`). Verified for real (not just the reported "completed" status): read the full rewritten `docs/README.md`, ran `bash run_tests.sh` (108 passed + 4 subtests, 1 legitimate skip), and caught/fixed two internal anchor links (`#6-telegram-optional` → `#4-telegram-optional`) the rewrite left stale after removing the sections above Telegram. Committed (`3ff1e37`) and pushed to SemAI's `migration-clean-up` branch.

## Bugs found and fixed this session

1. **`scripts/triapi.py` `cmd_plan()` approval-validation gap** — see above. Commit `f130296`, self-fix run `20260904-115032-017aeb`. Live-reproduced against the exact failure before and after the fix.
2. **That same fix's own verification was too narrow and broke an existing test** — see above. Commit `e503b79`, found during this session's own self-audit via a full `unittest discover` run instead of trusting the self-fix dispatch's "89/89 pass" report.

## Open / carried forward

- **SemAI Phase 8 (full agentic mode)** — recovered plan text exists (`git show 2a1e974^:plan.md` in the SemAI repo) but needs a rewrite pass for the current `src/semai/` layout (no Discord adapter, `ohmyllama/` paths gone) before it's dispatchable. Queued in SemAI's own `docs/Agent/CARRYOVER.md`, not TriAPI's.
- **`docs/README.md`'s two remaining cosmetic staleness spots** (SemAI): the `Install` section's `git clone`/`CALENDAR_SA_FILE` example paths still say `Oh-my-llama` — repo path/example only, not a feature claim, not worth another dispatch round on its own.
- The `agy` transient failure pattern (non-zero exit on first 1-2 attempts, succeeds on retry) is worth watching for recurrence — if it keeps happening on `tier_5_librarian`/`tier_1_planner` calls, it may warrant an actual retry-with-backoff inside `_call_agy_cli` rather than relying on the human noticing and re-running `triapi dispatch`.

## Next Steps

No implementation work pending in TriAPI itself. Next session should pick from: SemAI Phase 8 (needs a rewrite-then-plan pass, or continue normal supervision (self-fix backlog, tech-debt backlog).
